# Queued (not started)

## Price tier editor: tab-first redesign
User wants the NewAttractionModal price-tier editor restructured: user first defines
the available product TABS (e.g. "Galaxy", "Entry"), then per tab enters that tab's
tiers and visitor types, with prices editable via a dynamically-generated grid table
per tab (tier rows x visitor-type columns — mirrors the read-only pivot table just
built in AttractionDetailModal). Replaces the current flat list-of-tiers editor.
Queued after: seasonal opening-hours date ranges, admin edit-notification messages.
